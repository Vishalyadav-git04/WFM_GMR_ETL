version: '3.8'

services:
  api:
    build: .
    container_name: smart_meter_api
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
      - ./input_data:/app/input_data
    restart: always

# Note: This assumes a PostgreSQL database is already running externally.
# If you want to include a database in the compose file, uncomment below:
#  db:
#    image: postgres:15
#    container_name: smart_meter_db
#    environment:
#      - POSTGRES_DB=smart_meter_db
#      - POSTGRES_USER=postgres
#      - POSTGRES_PASSWORD=postgres
#    ports:
#      - "5432:5432"
#    volumes:
#      - postgres_data:/var/lib/postgresql/data

# volumes:
#   postgres_data:
